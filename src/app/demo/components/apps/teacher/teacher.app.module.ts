import { CommonModule } from "@angular/common";
import { NgModule } from "@angular/core";
import { TeacherAppRoutingModule } from "./teacher.app-routing.module";

@NgModule({
    imports: [
        CommonModule,
        TeacherAppRoutingModule
    ]
})
export class TeacherAppModule { }
